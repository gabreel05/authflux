import React from 'react'

import { Container, Indicator } from './styles'

const Loading: React.FC = () => (
  <Container>
    <Indicator size="large" color="#555" />
  </Container>
)

export default Loading
